from setuptools import setup

setup(
    name='snowflake',
    version='0.1',
    author='HAMED',
    author_email='hamed.mirbagher@fau.de',
    packages=['snowflake'],
    install_requires=['numpy','turtles'],
    )
